{
    "files": [{
        "from": "collectionview.coffee.hbs",
        "to": "app/views/{{name}}.coffee"
    }],

    "dependencies": [{
        "name": "collectionview",
        "params": "{{name}}"
    }]
}
